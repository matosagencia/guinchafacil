const { test } = require('@playwright/test');

test.describe('expiração de sessão legado', () => {
  test.skip(true, 'Cenário migrado para qa/suites/sessao-seguranca.spec.ts no runner oficial Playwright.');
});
